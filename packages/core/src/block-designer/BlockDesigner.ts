/**
 * BlockDesigner Class (DEPRECATED)
 *
 * This class will be replaced by a Zustand store in iteration 1.2.
 * Kept as a placeholder to maintain module structure.
 *
 * The new shape-centric model uses:
 * - Shape[] instead of Cell[]
 * - FabricRoles instead of color arrays
 * - UUID-based shape identification for undo/redo
 *
 * See: types.ts for the new data model
 */

// This file is intentionally minimal to pass typecheck.
// The full implementation will be a Zustand store (iteration 1.2).

export class BlockDesigner {
  // Placeholder - will be replaced by useBlockDesignerStore in iteration 1.2
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  constructor() {}
}
